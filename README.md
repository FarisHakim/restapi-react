## Create rest api express typescript

### Installation

1. Clone the repo
   ```sh
   git clone https://github.com/FarisHakim/restapi-react
   ```
2. Buka folder repo
   ```sh
   cd restapi-react
   ```
3. Run `npx tsc` in terminal
4. Run `npm run dev` in terminal
